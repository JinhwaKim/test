# OAuth 2.0 설정 가이드

Google Sheets API에 데이터를 쓰기 위해 OAuth 2.0 인증을 설정하는 방법입니다.

## 📋 OAuth 2.0이란?

OAuth 2.0은 사용자의 Google 계정으로 인증하여 Google API에 접근할 수 있게 해주는 인증 프로토콜입니다.

## 🚀 방법 1: 클라이언트 측 OAuth 2.0 (브라우저)

### 1단계: Google Cloud Console 설정

1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 프로젝트 선택 또는 새 프로젝트 생성
3. **API 및 서비스** > **사용 설정된 API** 이동
4. **+ API 및 서비스 사용 설정** 클릭
5. "Google Sheets API" 검색 후 **사용 설정** 클릭

### 2단계: OAuth 2.0 클라이언트 ID 생성

1. **API 및 서비스** > **사용자 인증 정보** 이동
2. **+ 사용자 인증 정보 만들기** > **OAuth 클라이언트 ID** 클릭
3. 동의 화면 구성 (처음인 경우):
   - 사용자 유형: **외부** 선택
   - 앱 이름: "TechSemiconductor 문의 폼" 입력
   - 사용자 지원 이메일: 본인 이메일
   - 개발자 연락처 정보: 본인 이메일
   - **저장 후 계속** 클릭
   - 범위: 기본값 유지, **저장 후 계속** 클릭
   - 테스트 사용자: 본인 이메일 추가, **저장 후 계속** 클릭
   - 요약 확인 후 **대시보드로 돌아가기** 클릭

4. OAuth 클라이언트 ID 생성:
   - 애플리케이션 유형: **웹 애플리케이션** 선택
   - 이름: "TechSemiconductor Web Client" 입력
   - 승인된 JavaScript 원본: 
     - `http://localhost:3000` (로컬 개발용)
     - `https://yourdomain.com` (프로덕션용)
   - 승인된 리디렉션 URI:
     - `http://localhost:3000/auth/callback` (로컬 개발용)
     - `https://yourdomain.com/auth/callback` (프로덕션용)
   - **만들기** 클릭
   - **클라이언트 ID**와 **클라이언트 보안 비밀번호** 복사 (나중에 필요)

### 3단계: 프론트엔드 코드 구현

`script-oauth2.js` 파일 참고

## 🔧 방법 2: 서버 측 OAuth 2.0 (권장)

서버에서 OAuth 2.0을 사용하는 것이 더 안전합니다.

### 1-2단계: 위와 동일 (Google Cloud Console 설정)

### 3단계: 서버 코드 구현

`server-oauth2.js` 파일 참고

## 📝 필요한 정보

설정 후 다음 정보가 필요합니다:

- **클라이언트 ID**: `xxxxx.apps.googleusercontent.com`
- **클라이언트 보안 비밀번호**: `GOCSPX-xxxxx`
- **리디렉션 URI**: `http://localhost:3000/auth/callback`

## 🔒 보안 주의사항

⚠️ **클라이언트 보안 비밀번호를 절대 공개하지 마세요!**
- 서버 측에서만 사용
- 환경 변수로 관리
- `.gitignore`에 추가

## 💡 추천 방법

**프로덕션 환경**: 서버 측 OAuth 2.0 또는 서비스 계정
**간단한 프로젝트**: Google Apps Script (현재 구현)

## 📚 참고 자료

- [Google OAuth 2.0 문서](https://developers.google.com/identity/protocols/oauth2)
- [Google Sheets API 문서](https://developers.google.com/sheets/api)

